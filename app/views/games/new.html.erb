<h1>New game</h1>
<p>what is the longest word you can find ?</p>
    <% @letters.each do |letter| %>
      <p class = "letter"><%= letter.upcase %></p>
    <% end %><form action="/score" method="post">
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <input type="text" name="word" placeholder="your word">
  <input type="submit" value="Play" class="play_button">
  <input name="letters" type="hidden" value=<%= @letters.join %>>
</form>
